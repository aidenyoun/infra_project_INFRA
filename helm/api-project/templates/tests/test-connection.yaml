apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "api-project.fullname" . }}-test-connection"
  labels:
    {{- include "api-project.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "api-project.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
